import React from 'react'


const HelpListInput = (props) => {
    return (
        <div>
            <input type="text" name={props.name} id={props.id}/>
        </div>
    )
}

export default HelpListInput;
